import { BookrideDirective } from './bookride.directive';

describe('BookrideDirective', () => {
  it('should create an instance', () => {
    const directive = new BookrideDirective();
    expect(directive).toBeTruthy();
  });
});
